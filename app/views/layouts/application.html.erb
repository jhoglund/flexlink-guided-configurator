<!DOCTYPE html>
<html class="h-full bg-white">
  <head>
    <title>FlexLink Configurator</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload", "data-timestamp": Time.current.to_i %>
    <%= javascript_importmap_tags %>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <!-- Inter font from Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700&display=swap" rel="stylesheet">
  </head>
  <body class="h-full bg-white">
    <!-- Header/Navigation -->
    <header class="bg-white border-b border-gray-200">
      <div class="max-w-[1400px] mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">
          <!-- Logo and Company Info -->
          <div class="flex items-center space-x-4">
            <div class="flex items-center space-x-2">
              <div class="w-8 h-4 bg-flexlink-red rounded-sm"></div>
              <span class="text-xl font-bold text-flexlink-red">FLEXLINK</span>
            </div>
            <span class="text-sm text-gray-500">a coesia company</span>
          </div>
          <!-- Main Navigation -->
          <nav class="hidden md:flex items-center space-x-8">
            <%= link_to "Home", root_path, class: "text-gray-700 hover:text-flexlink-red transition-colors" %>
            <div class="relative group">
              <a href="#" class="text-gray-700 hover:text-flexlink-red transition-colors flex items-center">
                Industries
                <i class="fas fa-chevron-down ml-1 text-xs"></i>
              </a>
            </div>
            <%= link_to "Systems", systems_path, class: "text-gray-700 hover:text-flexlink-red transition-colors" %>
            <a href="#" class="text-gray-700 hover:text-flexlink-red transition-colors">Build your own system</a>
            <a href="#" class="text-gray-700 hover:text-flexlink-red transition-colors">FAQ</a>
            <a href="#" class="text-gray-700 hover:text-flexlink-red transition-colors">Contact us</a>
          </nav>
          <!-- Right Side Actions -->
          <div class="flex items-center space-x-4">
            <!-- Language Selector -->
            <div class="flex items-center space-x-1 text-gray-700">
              <i class="fas fa-globe text-sm"></i>
              <span class="text-sm">Eng</span>
              <i class="fas fa-chevron-down text-xs"></i>
            </div>
            <!-- Icons -->
            <div class="flex items-center space-x-3">
              <button class="text-gray-700 hover:text-flexlink-red transition-colors">
                <i class="fas fa-search"></i>
              </button>
              <button class="text-gray-700 hover:text-flexlink-red transition-colors">
                <i class="fas fa-user"></i>
              </button>
              <button class="text-gray-700 hover:text-flexlink-red transition-colors relative">
                <i class="fas fa-shopping-cart"></i>
                <span class="absolute -top-1 -right-1 bg-flexlink-red text-white text-xs rounded-full w-4 h-4 flex items-center justify-center">0</span>
              </button>
            </div>
          </div>
        </div>
      </div>
    </header>
    <!-- Breadcrumbs -->
    <div class="bg-gray-50 border-b border-gray-200">
      <div class="max-w-[1400px] mx-auto px-4 sm:px-6 lg:px-8 py-2">
        <nav class="text-sm text-gray-500">
          <span>Home</span>
          <span class="mx-2">/</span>
          <span>E-Commerce Solutions</span>
          <span class="mx-2">/</span>
          <span>Configure your system</span>
        </nav>
      </div>
    </div>
    <!-- Main Content -->
    <main class="max-w-[1400px] mx-auto py-8 px-4 sm:px-6 lg:px-8">
      <!-- Flash Messages -->
      <% if notice %>
        <div class="mb-6 bg-green-50 border border-green-200 text-green-700 px-4 py-3 rounded-lg" role="alert">
          <span class="block sm:inline"><%= notice %></span>
        </div>
      <% end %>
      <% if alert %>
        <div class="mb-6 bg-flexlink-red bg-opacity-10 border border-flexlink-red text-flexlink-red px-4 py-3 rounded-lg" role="alert">
          <span class="block sm:inline"><%= alert %></span>
        </div>
      <% end %>
      <% flash.each do |name, msg| %>
        <% if msg.is_a?(String) %>
          <div class="mb-6 bg-<%= name == 'notice' ? 'green' : name == 'alert' ? 'flexlink-red bg-opacity-10' : 'blue' %>-50 border border-<%= name == 'notice' ? 'green' : name == 'alert' ? 'flexlink-red' : 'blue' %>-200 text-<%= name == 'notice' ? 'green' : name == 'alert' ? 'flexlink-red' : 'blue' %>-700 px-4 py-3 rounded-lg" role="alert">
            <span class="block sm:inline"><%= msg %></span>
          </div>
        <% end %>
      <% end %>
      <%= yield %>
    </main>
    <!-- Footer -->
    <footer class="bg-gray-900 text-white mt-16">
      <div class="max-w-[1400px] mx-auto py-12 px-4 sm:px-6 lg:px-8">
        <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
          <div>
            <div class="flex items-center space-x-2 mb-4">
              <div class="w-8 h-4 bg-flexlink-red rounded-sm"></div>
              <span class="text-xl font-bold">FLEXLINK</span>
            </div>
            <p class="text-gray-400 text-sm">We contribute to efficient production flows, maximize sellable throughput and minimize the TCO.</p>
          </div>
          <div>
            <h3 class="text-lg font-semibold mb-4">Products</h3>
            <ul class="space-y-2 text-gray-400 text-sm">
              <li><a href="#" class="hover:text-white transition-colors">Conveyor Systems</a></li>
              <li><a href="#" class="hover:text-white transition-colors">Automation Solutions</a></li>
              <li><a href="#" class="hover:text-white transition-colors">Integration Services</a></li>
            </ul>
          </div>
          <div>
            <h3 class="text-lg font-semibold mb-4">Industries</h3>
            <ul class="space-y-2 text-gray-400 text-sm">
              <li><a href="#" class="hover:text-white transition-colors">E-Commerce</a></li>
              <li><a href="#" class="hover:text-white transition-colors">Food & Beverage</a></li>
              <li><a href="#" class="hover:text-white transition-colors">Pharmaceutical</a></li>
            </ul>
          </div>
          <div>
            <h3 class="text-lg font-semibold mb-4">Contact</h3>
            <ul class="space-y-2 text-gray-400 text-sm">
              <li><a href="#" class="hover:text-white transition-colors">Contact Sales</a></li>
              <li><a href="#" class="hover:text-white transition-colors">Support</a></li>
              <li><a href="#" class="hover:text-white transition-colors">Careers</a></li>
            </ul>
          </div>
        </div>
        <div class="border-t border-gray-800 mt-8 pt-8 text-center text-gray-400 text-sm">
          <p>&copy; <%= Date.current.year %> FlexLink. All rights reserved.</p>
        </div>
      </div>
    </footer>
    <!-- Floating Help Widget -->
    <div class="fixed bottom-6 right-6 z-50">
      <div class="bg-white rounded-lg shadow-lg border border-gray-200 p-4 flex items-center space-x-3">
        <span class="text-gray-700 text-sm">Do you need help?</span>
        <button class="bg-flexlink-red text-white rounded-full w-8 h-8 flex items-center justify-center hover:bg-flexlink-red-dark transition-colors">
          <i class="fas fa-comments text-sm"></i>
        </button>
      </div>
    </div>
  </body>
</html> 